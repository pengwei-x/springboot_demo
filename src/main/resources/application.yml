server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://39.105.166.160:3306/springboot_demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root9876
    driver-class-name: com.mysql.jdbc.Driver
##热部署
  devtools:
    restart:
      enabled: true  #设置开启热部署
    freemarker:
      cache: false    #页面不加载缓存，修改即时生效

debug: true


# mybatis接口文件位置
mybatis.mapper-locations: classpath:mapper/*.xml
mybatis.type-aliases-package: com.pengwei.test01.springboot_demo.entity